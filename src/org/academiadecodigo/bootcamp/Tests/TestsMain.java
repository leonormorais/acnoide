package org.academiadecodigo.bootcamp.Tests;

import org.academiadecodigo.bootcamp.Screens.Game;
import org.academiadecodigo.bootcamp.Screens.Menu;
import org.academiadecodigo.bootcamp.Screens.PosGame;

public class TestsMain {

    private static String lastGame;
    private static boolean newGame = true;

    public static void main(String[] args) throws InterruptedException {
/*
        Menu m = new Menu();
        m.init();
        m.start();

        while (newGame) {
            Game g = new Game();
            g.init();
            g.start();
            lastGame = g.posGame();

            PosGame p = new PosGame(lastGame);
            p.init();
            p.start();
            newGame = p.restartInvoked();
        }

*/
    }
}
